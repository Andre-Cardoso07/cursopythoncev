# Crie um programa que leia as duas notas de um aluno, calcule a média e mostre na tela sua média.

nota1 = float(input('Qual é a primeira nota?'))
nota2 = float(input('Qual é a segunda nota?'))
media = (nota1+nota2)/2
print('A sua média é {}'.format(media))
